spring:
  application:
    name: userprofile-service #name of the application
  cloud:
    config:
      uri: http://localhost:8191
eureka:
  client:
    register-with-eureka: true 
    fetch-registry: true
  #instance:
   #  instance-id: ${spring.application.name}-${random.int}
    # hostname: localhost      